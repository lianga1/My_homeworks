class AudioPlayer {
    protected boolean openspeaker(Speaker sp){
        return true;
    }
}
class StreamAudioPlayer extends AudioPlayer{
    protected boolean openspeaker(Speaker sp){
        return true;
    }
}