#include <stdio.h>
#include "BTree.h"

/*
description: this program is to decode a huffman coding file and output the decoded file
*/

BTree* ExtendTree(char c, BTree* tree);
void decode(FILE* fp, BTree* tree);

int main()
{
    return 0;
}